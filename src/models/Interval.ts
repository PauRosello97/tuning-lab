export default interface Interval {
    num: number;
    denom: number;
    ratio: number;
    tenneyDistance: number;
    cents: number;
    numFactors: number[];
    denomFactors: number[];
    limit: number;
}