export default interface Interval {
    num: number;
    denom: number;
    ratio: number;
    distance: number;
    cents: number;
    limit: number;
    coordinates: number[];
    distanceCoordinates: number[];
}